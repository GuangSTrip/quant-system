> 当前请求强制携带官方 `X-Papertrading: true` 模拟环境保护头。除下述定投外，系统已提供注册日线组合自动调仓；两种执行器互斥。港股手动买卖、撤单、自动策略买卖及退出闭环已有实际模拟盘成交证据，当前验收范围以[最终交付说明](../docs/FINAL_DELIVERY.md)为准。

# 长桥模拟账户连接

入口：在线网站左侧「港股 · 长桥」（`#longbridge`）。使用已有课程账号登录，在网页输入开发者中心的 App Key、App Secret 和模拟账户 Access Token，勾选模拟账户声明，点击「验证连接并保存」。只读查询账户资金成功后才保存；失败保留原连接。保存后等待 5 秒，点击「刷新账户」读取资金、港股持仓及当日订单。

当前已提供独立长桥限价订单、撤单、订单详情与当日成交核对，以及定时定额限价买入策略。交易默认暂停；操作员在开发者中心确认模拟 Token 后，在本页输入“确认长桥模拟账户”并设置港币限额才能启用。授权与当前密文指纹绑定，换凭证后失效。接口未提供经本项目验证的模拟环境标识，系统仍返回 `environment_verified:false`，不以资金余额或 Token 前缀冒充验证。使用者必须确保只配置模拟账户；系统不提供实盘配置入口。

自动策略是 **定投执行策略**，不是美股双均线或动量策略移植。按指定间隔、股数、限价买入；总提交预算耗尽停止；待成交时不叠加委托；从空仓开始并检查策略持仓归属。香港常规时段以周一至周五 09:30–12:00 / 13:00–16:00 判断，交易所假期、半日市、停牌及价格档位由券商最终校验。没有实时行情推送或港股回测接入，不声称做行情择时。

## 凭证保存

站点秘密 `BROKER_CREDENTIAL_KEY` 是 32 字节随机值的 Base64 编码，通过托管设置配置，不进入仓库。可以在部署机器用 `node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"` 生成并保存至托管秘密。

三项用户凭证在服务器使用 AES-256-GCM、独立随机 nonce 和固定上下文 AAD 加密，D1 `longbridge_connection` 只保存密文、时间、操作员。密码字段不回显、不存入浏览器 storage，不进入审计、日志或导出。保存成功清空输入；失败保留输入供修正，退出登录清空。该连接为全站课程操作员共享，不是多租户账户。移除连接删除网站密文，不撤销券商 Token 或已有订单。轮换加密主密钥后需重新输入凭证，不可当作无影响的普通配置变更。

现有部署向导仍以 Alpaca 为主；独立部署长桥连接须额外配置上述主密钥，构建包含新增 Drizzle 迁移。未配置主密钥时保存按钮不可用，服务端也拒绝保存。不要将三项凭证写入 `.env.example` 或源码。

## 实现依据与验收

HTTP 地址固定 `https://openapi.longbridge.com`，使用官方 SDK 兼容的传统 API Key 签名（Authorization 直接携带 Access Token，非 OAuth Bearer）。算法参照官方 https://github.com/longbridge/openapi/blob/master/rust/crates/httpclient/src/signature.rs 和 request.rs。账户、持仓和当日订单接口参照 https://open.longbridge.com/zh-CN/docs/trade/asset/account 、https://open.longbridge.com/zh-CN/docs/trade/asset/stock 、https://open.longbridge.com/zh-CN/docs/trade/order/today_orders 。不自动跟随重定向，不发送到用户指定域名，不泄露供应商原始错误。

测试覆盖独立签名对照、AES-GCM 错误密钥与篡改拒绝、认证和 CSRF、保存前只读验证、密文存储、错误凭证保留原连接、移除、固定端点和重定向拒绝。额外 Workerd 测试验证生产运行时加密算法兼容性。自动化使用替身响应，实际凭证由账号持有人在部署后输入，因此不代表实际账户联调或成交已经通过。

## 2026-09-18 查询模块验收（交易模块上线前）

- 线上实际账户：独立课程账号登录成功；三次资金、港股持仓、港股当日订单查询成功；实际持仓和当日订单为空。页面切换后仍可查询，退出后清除账户数据。
- 修复：长桥/富途页隔离 Alpaca 顶部连接状态、交易开关、刷新时间、错误提示和通用刷新按钮；仅显示港股查询状态及本页刷新入口。
- 修复：刷新按钮从状态检查开始防重复提交；状态检查失败清除旧结果；宽表使用现有可横向滚动容器。
- 自动化：106 项通过，包括新增长桥非空数据渲染、部分失败、重复刷新、注销期间迟到响应、限流后恢复、无配置/损坏凭证、越权查询与删除、网络中断和错误脱敏。异常及非空订单场景使用隔离测试数据，未修改线上账户凭证。
- Worker 运行环境：SHA-1/HMAC 签名和 AES-GCM 测试通过；部署包验证通过。
- 未完成且不宣称通过：真实账户中非空订单/持仓核对；长桥下单、撤单、成交及自动策略执行（尚未实现）；仅凭资金查询无法独立证明账户为模拟环境。


## 交易操作与证据

1. 登录课程账号，打开「港股 · 长桥」，在“启用交易与调整限额”确认模拟账户并保存限额。长桥限额以 HKD 计，独立于 Alpaca 的 USD 限额。
2. 填写股票代码、券商公布的每手股数、股数和限价，先预览，再确认提交。休市需勾选允许排队。服务端检查现金（预留 1% 费用）、可卖股数、单笔/单日限额、已有委托和未知订单；不融资、不做空。
3. 下方平台委托显示唯一请求号和券商订单号。点击“核对订单与成交”查询券商状态、累计成交、当日成交明细及持仓变化；取得订单号不代表成交。撤单申请也须再次核对为 CanceledStatus / PartialWithdrawal。
4. 自动策略需启用交易、标的空仓、后台调度已配置。输入参数及“启动长桥自动模拟交易”。由原有 GitHub OIDC / Worker scheduled 通道运行，关闭网页不影响调度；可手动检查一轮。停止策略不自动撤销已有订单；“恢复原策略”保留原参数、累计预算和执行序号，不重新开一笔预算。

每笔请求先持久化再发往券商；本地请求号重复不再 POST，同时发送供应商 client_request_id 和 remark。超时保留 unknown 并阻断后续提交，通过订单号或 remark 查询当日/历史订单恢复，绝不自动重发。单日限额与策略总预算均按提交金额保守计量，撤单不退回额度。执行通道有互斥锁，中断后不自动接管；超时六分钟可人工恢复，恢复同时暂停交易和策略，未知委托仍须核对。

新增 D1 迁移 `0004` 为独立的 lb_control/lb_orders/lb_auto，不修改 Alpaca 账本。正在执行、存在未决委托或自动策略未暂停时禁止替换/删除连接。三个凭证仍不回显、不写入订单或审计。

## 交易模块测试与待验收

- 全套 124 项自动化测试通过，包含 POST/DELETE 独立签名向量、权限与 CSRF、港股格式与手数、现金/持仓/预算、并发互斥、重复提交、接收后超时恢复、数据库故障、撤单状态、成交/持仓差异、后台调度、自动预算停止和暂停竞态。
- 所有新增下单、成交、撤单和自动策略执行测试使用隔离券商替身；不作为真实长桥回报。
- 真实账户查询已验证；真实提交、撤单、成交及非空持仓对照仍需在线验收。验收时记录平台请求号、长桥订单号及成交号；休市只能验证排队/拒绝/撤销，不能宣称成交通过。
- 测试不会保证盈利，不会保证券商接受每个用户填写的标的、手数或价位；交易权限、行情权益和交易所约束由长桥最终决定。

官方交易接口依据：
- https://open.longbridge.com/zh-CN/docs/trade/order/submit
- https://open.longbridge.com/zh-CN/docs/trade/order/withdraw
- https://open.longbridge.com/zh-CN/docs/trade/order/order_detail
- https://open.longbridge.com/zh-CN/docs/trade/execution/today_executions


## 2026-09-19 兼容整合与在线证据更新

已与 main 的三市场日线研究共存，完整保留原研究文件；0003 与 0004 均为增量迁移。用户已在在线平台确认委托显示“已撤单”，因此记录该笔提交／撤单流程的人工核验通过；本轮未直接获取含订单号的券商回报，不将其当成自动化成交证据。买入成交、卖出成交、持仓资金一致性以及自动策略实际成交仍待开市验证。此前各节测试数字和未实现描述为对应日期的历史记录，当前结果以兼容整合记录和 CI 为准。
