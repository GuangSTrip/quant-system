{
  "name": "quant-system-paper-platform",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate-artifact.mjs",
    "test": "node --test --test-timeout=30000 tests/*.test.mjs",
    "db:generate": "drizzle-kit generate",
    "deploy": "node scripts/deploy.mjs",
    "deploy:verify": "node scripts/deploy.mjs --verify",
    "deploy:check": "node scripts/deploy.mjs --dry-run",
    "password:reset": "node scripts/deploy.mjs --reset-password",
    "test:runtime": "workerd test tests/workerd/transport.capnp && workerd test tests/workerd/auth.capnp",
    "local:setup": "node scripts/setup-local-paper.mjs",
    "local:migrate": "wrangler d1 migrations apply DB --local --config wrangler.local.json",
    "local:paper": "wrangler dev --local --ip 127.0.0.1 --port 8792 --config wrangler.local.json"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10",
    "drizzle-orm": "0.45.2",
    "esbuild": "0.28.0",
    "wrangler": "4.131.0"
  },
  "engines": {
    "node": ">=24"
  }
}
