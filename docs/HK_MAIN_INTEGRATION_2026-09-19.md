# main 与港股模拟交易兼容整合

基线：main `d02fe7a14182da9a3e1a644aef963b2c61de06dc`；港股分支 `13ebb9fabdc454809482cdc769356f3d909bbc3e`。后者的远端树不完整，因此不能采用其删除操作；以 main 全部 174 个文件为基础，仅迁入独立港股能力，并从已上线完整源码补回必要文件。

## 保留与新增

- 保留所有 main 文件。原 Python 研究、日线策略与组合、数据快照、主脚本和历史测试内容不变。
- 保留日线工作台、三市场历史曲线、分钟旧研究、美股回测、Alpaca Paper 交易、认证、风控和审计。
- 新增长桥独立账户连接、加密凭证、HKD 限额、限价提交、撤单、订单及持仓核对、未知订单恢复、定时定额限价买入与后台执行。
- 长桥运行记录和锁独立存储于 `lb_*`；原 Alpaca 表和迁移 0000—0002 保持不变。补齐增量迁移 0003（连接密文）和 0004（交易/自动执行）。
- 旧富途只读桥接保留，不在侧栏显示；日常港股交易使用「港股 · 长桥」。
- 保留原调度工作流与触发频率；调度处理器同时检查两个独立执行器。此合并不启动任何策略，不改变已有授权或预算。

## 策略边界

main 的三市场日线组合研究尚未连接券商执行。长桥自动功能是固定股数、固定限价和间隔的定投执行策略，不是研究页中的动态选股组合。不得把历史曲线或旧当日计划当作本次模拟盘成交证据；组合接入仍按 `PAPER_INTEGRATION_HANDOFF_2026-09-18.md` 后续实施。

## 本地验证

- Python 主测试 47 项、旧富途桥接测试 4 项通过。
- 网页既有和迁入测试 131 项通过；额外 2 项整合回归通过，共 133 项。
- 整合回归：从 main 数据库迁移后，原表结构及已保存研究、订单、风控和策略记录逐项一致；日线研究资源与需登录的长桥接口共存，读取不发送委托。
- 构建、ESM/Worker 导出校验通过；真实 Workerd 的传输、密码认证、长桥签名与加密三组测试通过。
- 交易测试使用隔离替身，不连接真实账户、不提交在线委托，不读取生产密钥。

## 部署及证据

新实例沿用 `web_platform/DEPLOY.md`，长桥还需服务端秘密 `BROKER_CREDENTIAL_KEY`，之后由操作员在网页配置模拟账户。已有长桥实例必须保留原加密主密钥和数据库，不重复生成密钥。未配置长桥的原实例仍可使用 Alpaca，长桥交易默认关闭。

用户已确认线上一笔委托显示“已撤单”；该结论为用户核验，非本轮直接取得完整券商回报。实际买入/卖出成交、持仓资金一致性和自动策略后台成交仍待开市验证。本地测试成功不替代这些证据。

仓库合并与线上发布分别核验，以实际 GitHub 提交和托管发布记录为准。
